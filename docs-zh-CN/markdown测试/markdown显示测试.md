---
title: markdown显示测试
description: 常见 Markdown 排版的实际效果与简短源码示例。
sidebar:
  label: markdown显示测试
  order: 10
---

本页展示标题、文字、列表、表格、代码和图片的 Markdown 样式，可以直接查看显示效果。🔎

## 标题层级

页面顶部的标题已经是一级标题（H1），正文从二级标题开始。

### 三级标题 H3

#### 四级标题 H4

##### 五级标题 H5

###### 六级标题 H6

```md
# 一级标题（本页由 title 提供）
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

## 段落与换行

这是一个普通段落。段落内可以连续书写多句话，浏览器会根据可用宽度自动换行。

这是第二个段落，源码中的空行将它与上一段分开。

这是需要明确换行的第一行。\
这是同一段中的第二行。

```md
第一段。

第二段的第一行。\
第二段的第二行。
```

## 文本样式

普通文字、**粗体**、*斜体*、***粗斜体***、~~删除线~~，以及行内代码 `pnpm build`。

```md
**粗体** *斜体* ***粗斜体*** ~~删除线~~ `pnpm build`
```

## 列表

- 无序列表第一项
- 无序列表第二项
  - 嵌套子项：补充说明
  - 嵌套子项：**突出重点**

1. 准备文档。
2. 预览效果。
   1. 检查文字与链接。
   2. 检查图片和代码。
3. 发布更新。

GFM 任务列表可以标记进度，下面展示的是写在文档中的静态内容：

- [x] 编写页面
- [x] 添加示例
- [ ] 按实际需要补充内容

```md
- [x] 已完成
- [ ] 待完成
```

## 引用与分隔线

> 这是一段引用，可以包含 **强调文字**。
>
> > 这是嵌套引用，用于展示更深一层的说明。

下面的横线用于分隔内容。写法是将 `---` 单独放在一行，前后各留一个空行。

---

分隔线之后可以继续写正文。✨

## 对齐表格

| 左对齐：样式 | 居中：状态 | 右对齐：数量 |
| :--- | :---: | ---: |
| **粗体文字** | 已完成 ✅ | 12 |
| *斜体文字* | 处理中 | 3 |
| `inline code` | 待处理 | 128 |

表头下一行用 `:---`、`:---:` 和 `---:` 分别指定左对齐、居中和右对齐。

## 多语言代码块

JavaScript：

```js
const title = "Nimbus Docs Template";
function greet(name) {
  return `你好，${name}！`;
}
console.log(greet(title));
```

JSON：

```json
{
  "schemaVersion": 1,
  "title": "markdown显示测试",
  "locale": "zh-CN"
}
```

Shell（仅展示命令，不会执行）：

```sh
pnpm install --frozen-lockfile
pnpm build
```

## 链接与图片

- 站内相对链接：[快速入门](../getting-started.md)。
- 同页锚点：[跳转到文本样式](#文本样式)。
- 外部链接：[项目 GitHub 仓库](https://github.com/Azincc/nimbus-docs-template.git)。

![Nimbus Docs Template 本地图片示例](../assets/nimbus-mark.svg)

```md
[快速入门](../getting-started.md)
[跳转到文本样式](#文本样式)
![本地图片](../assets/nimbus-mark.svg)
```

## 转义字符

\*这段文字保留星号\*，\[这段文字保留方括号\]，\# 也可以作为普通字符显示。

```md
\*保留星号\* \[保留方括号\] \# 普通井号
```
